; AiVids PHP-FPM pool tuning (8 GB VPS, shared with other services)
; ~12 workers × ~128 MB typical Laravel request ≈ 1.5 GB ceiling for this app
[www]
pm = dynamic
pm.max_children = 12
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8
pm.max_requests = 500
